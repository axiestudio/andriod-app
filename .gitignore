# Android / Gradle — local-only, never commit
local.properties
.gradle/
build/
app/build/
captures/
*.hprof

# Signing — NEVER commit: key material must not reach the public repo
*.jks
*.keystore
keystore.properties

# IDE
.idea/
*.iml
